diff -ruN a/.env.example b/.env.example
--- a/.env.example	2026-07-22 15:34:24.989337777 +0000
+++ b/.env.example	2026-07-22 17:30:14.297259535 +0000
@@ -18,6 +18,7 @@
 
 # Stripe — required when DEMO_MODE=false
 STRIPE_SECRET_KEY=
+STRIPE_API_VERSION=2025-03-31.basil
 STRIPE_WEBHOOK_SECRET=
 STRIPE_PRICE_BASIC_REPORT=
 STRIPE_PRICE_PRO_REPORT=
diff -ruN a/README.md b/README.md
--- a/README.md	2026-07-22 17:29:07.201260290 +0000
+++ b/README.md	2026-07-22 17:30:14.301259534 +0000
@@ -57,6 +57,7 @@
 ```dotenv
 DEMO_MODE=false
 STRIPE_SECRET_KEY=sk_...
+STRIPE_API_VERSION=2025-03-31.basil
 STRIPE_WEBHOOK_SECRET=whsec_...
 STRIPE_PRICE_BASIC_REPORT=price_...
 STRIPE_PRICE_PRO_REPORT=price_...
@@ -81,6 +82,8 @@
 
 The application verifies webhook signatures and records each checkout session only once before fulfilment.
 
+Managed Payments is enabled on every Checkout Session, so Stripe/Link acts as merchant of record for eligible products when the feature is activated in the Stripe Dashboard.
+
 ## Resend email configuration
 
 ```dotenv
diff -ruN a/RELEASE_NOTES.md b/RELEASE_NOTES.md
--- a/RELEASE_NOTES.md	2026-07-22 17:29:07.225260290 +0000
+++ b/RELEASE_NOTES.md	2026-07-22 17:30:14.301259534 +0000
@@ -50,3 +50,11 @@
 - End-to-end smoke test covering registration, assessment, private/public token isolation, paid fulfilment, PDF generation, subscription activation, account export, password recovery, password change and account deletion.
 
 Live Stripe, Resend, tax, legal, monitoring and independent security validation require production credentials or external professional review and are therefore launch-operator tasks rather than bundled code.
+
+## v1.1.1 — Stripe Managed Payments compatibility
+
+- Enables `managed_payments[enabled]=true` on every Stripe Checkout Session.
+- Pins Stripe API requests to `2025-03-31.basil`, the minimum Managed Payments-compatible API version.
+- Adds Render and `.env.example` configuration for `STRIPE_API_VERSION`.
+- Adds regression tests protecting the Managed Payments integration.
+
diff -ruN a/package.json b/package.json
--- a/package.json	2026-07-22 15:34:25.489337772 +0000
+++ b/package.json	2026-07-22 17:30:14.297259535 +0000
@@ -1,6 +1,6 @@
 {
   "name": "agent-risk-layer",
-  "version": "1.1.0",
+  "version": "1.1.1",
   "private": true,
   "type": "module",
   "description": "Launch-ready beta for automated AI-agent security assessments and professional reports.",
diff -ruN a/render.yaml b/render.yaml
--- a/render.yaml	2026-07-22 15:34:25.597337770 +0000
+++ b/render.yaml	2026-07-22 17:30:14.297259535 +0000
@@ -31,6 +31,8 @@
         sync: false
       - key: STRIPE_SECRET_KEY
         sync: false
+      - key: STRIPE_API_VERSION
+        value: 2025-03-31.basil
       - key: STRIPE_WEBHOOK_SECRET
         sync: false
       - key: STRIPE_PRICE_BASIC_REPORT
diff -ruN a/server.js b/server.js
--- a/server.js	2026-07-22 15:39:51.009334107 +0000
+++ b/server.js	2026-07-22 17:30:14.297259535 +0000
@@ -350,6 +350,7 @@
     params.set('mode', plan.recurring ? 'subscription' : 'payment');
     params.set('line_items[0][price]', price);
     params.set('line_items[0][quantity]', '1');
+    params.set('managed_payments[enabled]', 'true');
     params.set('customer_email', req.user.email);
     params.set('client_reference_id', req.user.id);
     params.set('allow_promotion_codes', 'true');
@@ -555,7 +556,7 @@
   if (!config.stripeSecretKey) throw new Error('Stripe secret key is missing.');
   const response = await fetch(`https://api.stripe.com${endpoint}`, {
     method,
-    headers: { Authorization: `Bearer ${config.stripeSecretKey}`, ...(params ? { 'Content-Type': 'application/x-www-form-urlencoded' } : {}) },
+    headers: { Authorization: `Bearer ${config.stripeSecretKey}`, 'Stripe-Version': config.stripeApiVersion, ...(params ? { 'Content-Type': 'application/x-www-form-urlencoded' } : {}) },
     body: params ? params.toString() : undefined,
   });
   const payload = await response.json();
diff -ruN a/src/config.js b/src/config.js
--- a/src/config.js	2026-07-22 15:30:19.253340544 +0000
+++ b/src/config.js	2026-07-22 17:30:14.297259535 +0000
@@ -4,7 +4,7 @@
 const defaultSessionSecret = 'development-only-change-this-secret-before-deployment-123456';
 
 export const config = {
-  appVersion: '1.1.0',
+  appVersion: '1.1.1',
   scoringVersion: 'arl-risk-v1.1',
   termsVersion: process.env.TERMS_VERSION || '2026-07-22',
   companyName: process.env.COMPANY_NAME || 'AgentRiskLayer',
@@ -20,6 +20,7 @@
   sessionSecret: process.env.SESSION_SECRET || defaultSessionSecret,
   databasePath: path.resolve(root, process.env.DATABASE_PATH || './data/agent-risk-layer.sqlite'),
   stripeSecretKey: process.env.STRIPE_SECRET_KEY || '',
+  stripeApiVersion: process.env.STRIPE_API_VERSION || '2025-03-31.basil',
   stripeWebhookSecret: process.env.STRIPE_WEBHOOK_SECRET || '',
   resendApiKey: process.env.RESEND_API_KEY || '',
   emailFrom: process.env.EMAIL_FROM || 'AgentRiskLayer <reports@example.com>',
diff -ruN a/tests/managed-payments.test.js b/tests/managed-payments.test.js
--- a/tests/managed-payments.test.js	1970-01-01 00:00:00.000000000 +0000
+++ b/tests/managed-payments.test.js	2026-07-22 17:30:14.301259534 +0000
@@ -0,0 +1,18 @@
+import test from 'node:test';
+import assert from 'node:assert/strict';
+import fs from 'node:fs';
+import path from 'node:path';
+
+const root = path.resolve(import.meta.dirname, '..');
+
+test('Stripe Checkout explicitly enables Managed Payments', () => {
+  const source = fs.readFileSync(path.join(root, 'server.js'), 'utf8');
+  assert.match(source, /managed_payments\[enabled\][^\n]*true/);
+});
+
+test('Stripe requests pin a Managed Payments-compatible API version', () => {
+  const server = fs.readFileSync(path.join(root, 'server.js'), 'utf8');
+  const config = fs.readFileSync(path.join(root, 'src', 'config.js'), 'utf8');
+  assert.match(server, /Stripe-Version/);
+  assert.match(config, /2025-03-31\.basil/);
+});
diff -ruN a/tests/smoke.mjs b/tests/smoke.mjs
--- a/tests/smoke.mjs	2026-07-22 15:41:35.057332935 +0000
+++ b/tests/smoke.mjs	2026-07-22 17:30:14.301259534 +0000
@@ -92,7 +92,7 @@
 try {
   const health = await wait();
   assert.equal(health.ok, true);
-  assert.equal(health.version, '1.1.0');
+  assert.equal(health.version, '1.1.1');
 
   await request('/api/auth/register', { method: 'POST', body: { email: 'owner@example.com', password: 'secure-demo-password', termsAccepted: true } });
   const { questionnaire } = await request('/api/questionnaire');
