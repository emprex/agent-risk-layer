{
  "name": "agent-risk-layer",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "description": "Launch-ready beta for automated AI-agent security assessments and professional reports.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test",
    "check": "node --check server.js && for f in src/*.js public/*.js; do node --check \"$f\"; done",
    "smoke": "node tests/smoke.mjs",
    "validate": "npm test && npm run check && npm run smoke"
  },
  "dependencies": {}
}
