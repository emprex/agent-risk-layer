import fs from 'node:fs';

function replaceOnce(source, before, after, label) {
  if (!source.includes(before)) throw new Error(`Patch marker not found: ${label}`);
  const next = source.replace(before, after);
  if (next === source) throw new Error(`Patch did not change source: ${label}`);
  return next;
}

const controlPath = 'src/control-plane.js';
let control = fs.readFileSync(controlPath, 'utf8');

const helperMarker = `export async function updateRemediationItem({ projectId, itemId, userId, patch = {} }) {\n`;
const helper = `function inspectionFindingActive(findings, ruleId) {\n  return (Array.isArray(findings) ? findings : []).some((finding) =>\n    clean(finding?.ruleId, 40) === ruleId && finding?.review?.status !== 'false-positive');\n}\n\nasync function verifiedObservedInspectionClosure({ access, current, userId, input, verifiedAt }) {\n  if (!current.assessment_id) throw badRequest('Observed Inspector closure requires an assessment-bound remediation.');\n  if (!input || typeof input !== 'object' || Array.isArray(input))\n    throw badRequest('Observed Inspector closure evidence is required.');\n  const inspectionId = clean(input.inspectionId, 100);\n  const ruleId = clean(input.ruleId, 40);\n  const findingRuleId = clean(String(current.finding_key || '').split(':').at(-1), 40);\n  if (!inspectionId || !ruleId || ruleId !== findingRuleId)\n    throw badRequest('Observed Inspector closure must match this remediation finding.');\n\n  const latest = await db.prepare(\`SELECT id,assessment_id,scanner_version,policy_version,bundle_digest,signature_valid,delta_json,findings_json,created_at\n    FROM inspections WHERE assessment_id=? ORDER BY created_at DESC LIMIT 1\`).get(current.assessment_id);\n  if (!latest || latest.id !== inspectionId || !latest.signature_valid || !safeEqualDigest(latest.bundle_digest, latest.bundle_digest))\n    throw badRequest('The requested Inspector retest is not the latest integrity-verified inspection for this assessment.');\n  if (Date.parse(latest.created_at || '') <= Date.parse(current.created_at || ''))\n    throw badRequest('The Inspector retest must be newer than the remediation record.');\n\n  const delta = parseJson(latest.delta_json, {});\n  const resolved = Array.isArray(delta.resolvedFindings) ? delta.resolvedFindings : [];\n  if (!delta.baselineInspectionId || !resolved.some((key) => String(key || '').startsWith(\`${ruleId}:\`)))\n    throw badRequest('The latest Inspector retest does not resolve this remediation rule.');\n  const baseline = await db.prepare(\`SELECT id,assessment_id,signature_valid,findings_json FROM inspections\n    WHERE id=? AND assessment_id=?\`).get(delta.baselineInspectionId, current.assessment_id);\n  if (!baseline || !baseline.signature_valid) throw badRequest('The Inspector baseline for this retest is missing or invalid.');\n  const baselineFindings = parseJson(baseline.findings_json, []);\n  const latestFindings = parseJson(latest.findings_json, []);\n  if (!inspectionFindingActive(baselineFindings, ruleId) || inspectionFindingActive(latestFindings, ruleId))\n    throw badRequest('The Inspector before/after evidence does not support closure of this rule.');\n\n  const previous = parseJson(current.verification_json, {});\n  const history = Array.isArray(previous.history) ? [...previous.history] : [];\n  history.push({\n    action: 'observed_inspection_retest_accepted', actorId: userId, at: verifiedAt,\n    inspectionId: latest.id, baselineInspectionId: baseline.id, ruleId,\n  });\n  return {\n    ...previous,\n    retestResult: 'passed',\n    retestReference: \`Inspector retest \${latest.id}\`,\n    retestArtifactId: latest.id,\n    retestArtifactDigest: latest.bundle_digest,\n    retestArtifactVerifiedAt: verifiedAt,\n    retestArtifactEvidenceType: 'verified_artifact',\n    retestSourceType: 'inspection',\n    retestRuleId: ruleId,\n    retestBaselineInspectionId: baseline.id,\n    retestScannerVersion: latest.scanner_version,\n    retestPolicyVersion: latest.policy_version,\n    retestedAt: latest.created_at,\n    verifiedAt,\n    history,\n  };\n}\n\nexport async function updateRemediationItem({ projectId, itemId, userId, patch = {} }) {\n`;
control = replaceOnce(control, helperMarker, helper, 'observed inspection closure helper');

const transitionBefore = `  const status = patch.status == null ? currentStatus : clean(patch.status, 40).toLowerCase();\n  if (!REMEDIATION_STATUSES.has(status)) throw badRequest('Unknown remediation status.');\n  if (status !== currentStatus && !REMEDIATION_TRANSITIONS[currentStatus]?.has(status))\n    throw badRequest(\`Remediation cannot move from \${currentStatus} to \${status}. Complete the required evidence and retest step first.\`);\n`;
const transitionAfter = `  const status = patch.status == null ? currentStatus : clean(patch.status, 40).toLowerCase();\n  if (!REMEDIATION_STATUSES.has(status)) throw badRequest('Unknown remediation status.');\n  const observedClosureInput = patch?.verification?.observedInspectionClosure;\n  const observedInspectionClosureRequested = status === 'verified_closed' && currentStatus === 'open'\n    && observedClosureInput && typeof observedClosureInput === 'object' && !Array.isArray(observedClosureInput);\n  if (status !== currentStatus && !REMEDIATION_TRANSITIONS[currentStatus]?.has(status) && !observedInspectionClosureRequested)\n    throw badRequest(\`Remediation cannot move from \${currentStatus} to \${status}. Complete the required evidence and retest step first.\`);\n`;
control = replaceOnce(control, transitionBefore, transitionAfter, 'observed closure transition gate');

const verificationBefore = `  const previousVerification = parseJson(current.verification_json, {});\n  let verification = patch.verification == null ? previousVerification : { ...previousVerification, ...sanitiseVerificationInput(patch.verification) };\n  const transitionAt = nowIso();\n`;
const verificationAfter = `  const previousVerification = parseJson(current.verification_json, {});\n  let verification = patch.verification == null ? previousVerification : { ...previousVerification, ...sanitiseVerificationInput(patch.verification) };\n  const transitionAt = nowIso();\n  if (observedInspectionClosureRequested)\n    verification = await verifiedObservedInspectionClosure({ access, current, userId, input: observedClosureInput, verifiedAt: transitionAt });\n`;
control = replaceOnce(control, verificationBefore, verificationAfter, 'observed closure verification');

const auditBefore = `  await audit({ workspaceId: access.project.workspace_id, projectId, actorType: 'user', actorId: userId, action: 'remediation.updated', targetType: 'remediation', targetId: itemId, metadata: { status, severity } });\n`;
const auditAfter = `  if (observedInspectionClosureRequested) {\n    await audit({ workspaceId: access.project.workspace_id, projectId, actorType: 'user', actorId: userId,\n      action: 'remediation.observed_retest_accepted', targetType: 'remediation', targetId: itemId,\n      metadata: { assessmentId: current.assessment_id, inspectionId: verification.retestArtifactId,\n        baselineInspectionId: verification.retestBaselineInspectionId, ruleId: verification.retestRuleId } });\n  }\n  await audit({ workspaceId: access.project.workspace_id, projectId, actorType: 'user', actorId: userId, action: 'remediation.updated', targetType: 'remediation', targetId: itemId, metadata: { status, severity } });\n`;
control = replaceOnce(control, auditBefore, auditAfter, 'observed closure audit');

const listBefore = `  return rows.map((row) => {\n    const item = publicRemediation(row);\n    const implementation = byId.get(item.verification.artifactId);\n    const retest = byId.get(item.verification.retestArtifactId);\n    item.trustedImplementationEvidence = implementation?.artifact_type === 'implementation'\n      && implementation.remediation_id === item.id && safeEqualDigest(implementation.content_digest, item.verification.artifactDigest);\n    item.trustedRetestEvidence = retest?.artifact_type === 'retest' && retest.remediation_id === item.id\n      && retest.source_event_id && retest.event_remediation_id === item.id && Boolean(retest.retest_satisfied)\n      && retest.retest_criteria_id === item.verification.retestCriteriaId\n      && safeEqualDigest(retest.retest_criteria_digest, item.verification.retestCriteriaDigest)\n      && safeEqualDigest(retest.content_digest, item.verification.retestArtifactDigest);\n    return item;\n  });\n`;
const listAfter = `  return Promise.all(rows.map(async (row) => {\n    const item = publicRemediation(row);\n    const implementation = byId.get(item.verification.artifactId);\n    const retest = byId.get(item.verification.retestArtifactId);\n    item.trustedImplementationEvidence = implementation?.artifact_type === 'implementation'\n      && implementation.remediation_id === item.id && safeEqualDigest(implementation.content_digest, item.verification.artifactDigest);\n    item.trustedRetestEvidence = retest?.artifact_type === 'retest' && retest.remediation_id === item.id\n      && retest.source_event_id && retest.event_remediation_id === item.id && Boolean(retest.retest_satisfied)\n      && retest.retest_criteria_id === item.verification.retestCriteriaId\n      && safeEqualDigest(retest.retest_criteria_digest, item.verification.retestCriteriaDigest)\n      && safeEqualDigest(retest.content_digest, item.verification.retestArtifactDigest);\n    if (item.verification.retestSourceType === 'inspection' && item.assessment_id && item.verification.retestArtifactId) {\n      const inspection = await db.prepare(\`SELECT id,assessment_id,bundle_digest,signature_valid,delta_json,findings_json FROM inspections\n        WHERE id=? AND assessment_id=?\`).get(item.verification.retestArtifactId, item.assessment_id);\n      const delta = parseJson(inspection?.delta_json, {});\n      const ruleId = clean(item.verification.retestRuleId, 40);\n      const resolved = Array.isArray(delta.resolvedFindings) ? delta.resolvedFindings : [];\n      item.trustedRetestEvidence = Boolean(inspection?.signature_valid)\n        && safeEqualDigest(inspection?.bundle_digest, item.verification.retestArtifactDigest)\n        && delta.baselineInspectionId === item.verification.retestBaselineInspectionId\n        && resolved.some((key) => String(key || '').startsWith(\`${ruleId}:\`))\n        && !inspectionFindingActive(parseJson(inspection.findings_json, []), ruleId);\n    }\n    return item;\n  }));\n`;
control = replaceOnce(control, listBefore, listAfter, 'inspection retest trust');

const remediationEvidenceBefore = `  const hasRemediationEvidence = (project.remediations || []).some((item) => {\n    const verification = item.verification || {};\n    return ['evidence_attached', 'ready_for_retest', 'retested', 'verified_closed'].includes(normaliseRemediationStatus(item.status))\n      && verification.artifactEvidenceType === 'verified_artifact'\n      && item.trustedImplementationEvidence === true\n      && Boolean(verification.artifactId && verification.artifactDigest && verification.artifactVerifiedAt && verification.evidenceAttachedAt);\n  });\n`;
const remediationEvidenceAfter = `  const hasRemediationEvidence = (project.remediations || []).some((item) => {\n    const verification = item.verification || {};\n    const boundedInspectionClosure = normaliseRemediationStatus(item.status) === 'verified_closed'\n      && verification.retestSourceType === 'inspection' && verification.retestResult === 'passed'\n      && item.trustedRetestEvidence === true;\n    return boundedInspectionClosure || (['evidence_attached', 'ready_for_retest', 'retested', 'verified_closed'].includes(normaliseRemediationStatus(item.status))\n      && verification.artifactEvidenceType === 'verified_artifact'\n      && item.trustedImplementationEvidence === true\n      && Boolean(verification.artifactId && verification.artifactDigest && verification.artifactVerifiedAt && verification.evidenceAttachedAt));\n  });\n`;
control = replaceOnce(control, remediationEvidenceBefore, remediationEvidenceAfter, 'inspection remediation evidence readiness');

fs.writeFileSync(controlPath, control);

const cardsPath = 'public/observed-remediation-cards.js';
let cards = fs.readFileSync(cardsPath, 'utf8');

const cardStart = `function observedCard(finding, remediation, { noLongerReproduced = false, latestInspection = null } = {}) {\n`;
cards = replaceOnce(cards, cardStart, `function observedCard(finding, remediation, { noLongerReproduced = false, latestInspection = null } = {}) {\n`, 'card function marker');

cards = replaceOnce(cards,
`  const description = finding?.message || finding?.description || 'The authorised static inspection observed this condition in the assessed system.';\n  const impact = ruleId === 'ARL-AI-006'\n    ? 'Malformed, hallucinated or attacker-influenced model output can reach a tool or business action and trigger unintended parameters or actions.'\n    : ruleId === 'ARL-AI-005'\n      ? 'Unbounded agent execution can cause runaway cost, degraded service or uncontrolled retries and tool activity.'\n      : 'The observed condition can weaken the control boundary described by this finding.';\n  const summaryAction = noLongerReproduced ? 'Review retest' : 'Open fix';\n`,
`  const description = finding?.message || finding?.description || 'The authorised static inspection observed this condition in the assessed system.';\n  const impact = ruleId === 'ARL-AI-006'\n    ? 'Model-produced output can reach a tool or business action without an independent validation boundary.'\n    : ruleId === 'ARL-AI-005'\n      ? 'Agent execution can continue without explicit time, token, retry, tool-depth, concurrency or spend limits.'\n      : 'The observed condition can weaken the control boundary described by this finding.';\n  const whyItMatters = ruleId === 'ARL-AI-006'\n    ? 'Malformed, hallucinated or attacker-influenced arguments can trigger the wrong refund, destination, parameters or other unintended business action.'\n    : ruleId === 'ARL-AI-005'\n      ? 'Runaway execution can create unexpected cost, service exhaustion, degraded availability or uncontrolled repeated tool activity.'\n      : description;\n  const inspectionClosed = remediation?.status === 'verified_closed' && remediation?.verification?.retestSourceType === 'inspection';\n  const summaryAction = inspectionClosed ? 'Closure recorded' : noLongerReproduced ? 'Review retest' : 'Open fix';\n`, 'finding impact copy and closure state');

cards = replaceOnce(cards,
`  const verification = noLongerReproduced\n    ? 'The latest comparable inspection no longer reproduces this rule. Keep the remediation open until an accountable review accepts the bounded retest evidence and records closure.'\n    : 'This remediation remains open until bounded retest evidence supports closure. Assignment or implementation alone is not verification.';\n`,
`  const verification = inspectionClosed\n    ? 'An accountable reviewer accepted this bounded Inspector retest. The closure applies to this exact static finding and does not prove complete runtime safety.'\n    : noLongerReproduced\n      ? 'The latest comparable inspection no longer reproduces this rule. Keep the remediation open until an accountable review accepts the bounded retest evidence and records closure.'\n      : 'This remediation remains open until bounded retest evidence supports closure. Assignment or implementation alone is not verification.';\n`, 'verification copy');

cards = replaceOnce(cards,
`  return \`<details class="remediation-row observed-remediation-card\${noLongerReproduced ? ' observed-retest-ready' : ''}" data-remediation-id="\${escapeHtml(remediation.id)}">\n    <summary>\n      <span class="status-pill">\${escapeHtml(finding?.severity || remediation?.severity || 'medium')}</span>\n      <span><strong>\${escapeHtml(ruleId)} · \${escapeHtml(title)}</strong><small>\${escapeHtml(noLongerReproduced ? 'Retest evidence available' : statusLabel(remediation))} · owner \${escapeHtml(remediation?.owner_email || 'unassigned')}</small></span>\n      <span>\${escapeHtml(summaryAction)}</span>\n    </summary>\n    <div class="remediation-body">\n      <p><strong>What can happen</strong><br>\${escapeHtml(impact)}</p>\n      <p><strong>Why it matters</strong><br>\${escapeHtml(impact)}</p>\n      <p><strong>Observed evidence</strong><br>\${escapeHtml(evidenceText)}</p>\n      \${scanMeta}\n      <p><strong>Fix</strong><br>\${escapeHtml(finding?.remediation || remediation?.title || 'Remediate the observed weakness.')}</p>\n      <p><strong>Owner</strong><br>\${escapeHtml(remediation?.owner_email || 'Unassigned')}</p>\n      <p><strong>Exact retest</strong><br>\${escapeHtml(exactRetest(finding))}</p>\n      <div class="notice"><strong>Verification rule</strong><br>\${escapeHtml(verification)}</div>\n    </div>\n  </details>\`;\n`,
`  const closureAction = noLongerReproduced && !inspectionClosed && latestInspection?.id\n    ? \`<div class="notice success observed-closure-review"><strong>Accountable closure review</strong><p>Accept only if this latest bounded static retest is the evidence you intend to rely on for this exact finding. This does not prove runtime behaviour or unrelated controls.</p><button class="button primary" type="button" data-accept-observed-retest data-remediation-id="\${escapeHtml(remediation.id)}" data-inspection-id="\${escapeHtml(latestInspection.id)}" data-rule-id="\${escapeHtml(ruleId)}">Accept retest evidence and close finding</button><p class="error-box" data-observed-closure-error hidden></p></div>\`\n    : inspectionClosed\n      ? \`<div class="notice success"><strong>Verified closed</strong><br>Closure review recorded against Inspector \${escapeHtml(remediation?.verification?.retestArtifactId || latestInspection?.id || '')}. Reassess if the model, tools, permissions, data, prompts or environment change.</div>\`\n      : '';\n  return \`<details class="remediation-row observed-remediation-card\${noLongerReproduced ? ' observed-retest-ready' : ''}" data-remediation-id="\${escapeHtml(remediation.id)}">\n    <summary>\n      <span class="status-pill">\${escapeHtml(finding?.severity || remediation?.severity || 'medium')}</span>\n      <span><strong>\${escapeHtml(ruleId)} · \${escapeHtml(title)}</strong><small>\${escapeHtml(inspectionClosed ? 'Verified closed' : noLongerReproduced ? 'Retest evidence available' : statusLabel(remediation))} · owner \${escapeHtml(remediation?.owner_email || 'unassigned')}</small></span>\n      <span>\${escapeHtml(summaryAction)}</span>\n    </summary>\n    <div class="remediation-body">\n      <p><strong>What can happen</strong><br>\${escapeHtml(impact)}</p>\n      <p><strong>Why it matters</strong><br>\${escapeHtml(whyItMatters)}</p>\n      <p><strong>Observed evidence</strong><br>\${escapeHtml(evidenceText)}</p>\n      \${scanMeta}\n      <p><strong>Fix</strong><br>\${escapeHtml(finding?.remediation || remediation?.title || 'Remediate the observed weakness.')}</p>\n      <p><strong>Owner</strong><br>\${escapeHtml(remediation?.owner_email || 'Unassigned')}</p>\n      <p><strong>Exact retest</strong><br>\${escapeHtml(exactRetest(finding))}</p>\n      <div class="notice"><strong>Verification rule</strong><br>\${escapeHtml(verification)}</div>\n      \${closureAction}\n    </div>\n  </details>\`;\n`, 'closure review action');

const appendBefore = `    list.appendChild(container);\n  } catch {\n`;
const appendAfter = `    list.appendChild(container);\n    container.querySelectorAll('[data-accept-observed-retest]').forEach((button) => {\n      button.addEventListener('click', async () => {\n        if (button.disabled) return;\n        const remediationId = button.dataset.remediationId || '';\n        const inspectionId = button.dataset.inspectionId || '';\n        const ruleId = button.dataset.ruleId || '';\n        const errorBox = button.parentElement?.querySelector('[data-observed-closure-error]');\n        button.disabled = true;\n        button.textContent = 'Recording closure…';\n        if (errorBox) { errorBox.hidden = true; errorBox.textContent = ''; }\n        try {\n          await api(\`/api/projects/\${encodeURIComponent(projectId)}/remediations/\${encodeURIComponent(remediationId)}\`, {\n            method: 'PATCH',\n            body: JSON.stringify({\n              status: 'verified_closed',\n              verification: { observedInspectionClosure: { inspectionId, ruleId } },\n            }),\n          });\n          location.reload();\n        } catch (error) {\n          button.disabled = false;\n          button.textContent = 'Accept retest evidence and close finding';\n          if (errorBox) {\n            errorBox.hidden = false;\n            errorBox.classList.add('show');\n            errorBox.textContent = error?.message || 'Could not record the closure review.';\n          }\n        }\n      });\n    });\n  } catch {\n`;
cards = replaceOnce(cards, appendBefore, appendAfter, 'closure action binding');
fs.writeFileSync(cardsPath, cards);

const testPath = 'tests/observed-remediation-retest.test.js';
let testSource = fs.readFileSync(testPath, 'utf8');
testSource = testSource.replace(
`  assert.doesNotMatch(source, /status\\s*=\\s*['\"]verified_closed['\"]/);\n`,
`  assert.match(source, /Accept retest evidence and close finding/);\n  assert.match(source, /observedInspectionClosure/);\n  assert.match(source, /status: 'verified_closed'/);\n`);
fs.writeFileSync(testPath, testSource);

fs.writeFileSync('tests/observed-remediation-closure-gate.test.js', `import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport fs from 'node:fs';\n\nconst backend = fs.readFileSync(new URL('../src/control-plane.js', import.meta.url), 'utf8');\nconst client = fs.readFileSync(new URL('../public/observed-remediation-cards.js', import.meta.url), 'utf8');\n\ntest('observed Inspector closure is server-gated by the latest before/after evidence', () => {\n  assert.match(backend, /verifiedObservedInspectionClosure/);\n  assert.match(backend, /latest integrity-verified inspection/);\n  assert.match(backend, /delta\\.baselineInspectionId/);\n  assert.match(backend, /resolvedFindings/);\n  assert.match(backend, /inspectionFindingActive\\(baselineFindings, ruleId\\)/);\n  assert.match(backend, /inspectionFindingActive\\(latestFindings, ruleId\\)/);\n  assert.match(backend, /remediation\\.observed_retest_accepted/);\n});\n\ntest('observed closure remains an explicit human action and preserves bounded claims', () => {\n  assert.match(client, /Accountable closure review/);\n  assert.match(client, /Accept retest evidence and close finding/);\n  assert.match(client, /does not prove runtime behaviour/);\n  assert.match(client, /Verified closed/);\n});\n\ntest('inspection retest evidence is revalidated for readiness rather than trusted from client metadata', () => {\n  assert.match(backend, /retestSourceType === 'inspection'/);\n  assert.match(backend, /safeEqualDigest\\(inspection\\?\\.bundle_digest, item\\.verification\\.retestArtifactDigest\\)/);\n  assert.match(backend, /item\\.trustedRetestEvidence = Boolean\\(inspection\\?\\.signature_valid\\)/);\n  assert.match(backend, /boundedInspectionClosure/);\n});\n`);

console.log('Applied observed Inspector closure-review patch.');
