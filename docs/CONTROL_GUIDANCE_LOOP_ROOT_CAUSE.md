Root-cause summary: `MutationObserver(enhance)` observed subtree child-list changes; `enhanceTest()` called `updateTestButton()`; `updateTestButton()` rewrote option/button `textContent` on every callback; those writes generated new child-list mutations. Immediate containment is to stop loading that guidance module on the focused control page while preserving the core evidence workflow.