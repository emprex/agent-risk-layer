import { api, escapeHtml, qs } from './shared.js';const root=document.querySelector('#runRoot');
async function init(){const id=qs('id');if(!id)return fail('Run ID is missing.');try{const{run}=await api(`/api/redteam/runs/${encodeURIComponent(id)}`);render(run)}catch(error){if(error.message.includes('Sign in'))location.href=`/auth.html?next=${encodeURIComponent(location.pathname+location.search)}`;else fail(error.message)}}
function render(run){const s=run.summary,simulation=run.campaign?.target?.mode==='simulation';root.className='';root.innerHTML=`<div class="page-heading"><span class="eyebrow">${simulation?'Pipeline simulation':'Controlled staging evidence'}</span><h1>${escapeHtml(run.campaign?.name||'Red-team campaign')}</h1><p>${simulation?'This run verifies the test harness and upload pipeline. It must not be interpreted as evidence about the assessed system.':'These results were generated by a customer-operated runner against an authorised staging adapter using synthetic data and dry-run tools.'}</p></div><div class="dashboard-stats"><div class="stat"><span>Assurance</span><strong>${s.assuranceScore}/100</strong></div><div class="stat"><span>Adversarial risk</span><strong>${s.riskScore}/100</strong></div><div class="stat"><span>Passed</span><strong>${s.counts.passed}</strong></div><div class="stat"><span>Failed</span><strong>${s.counts.failed}</strong></div><div class="stat"><span>Inconclusive/error</span><strong>${s.counts.inconclusive+s.counts.error}</strong></div></div><section class="panel"><div class="decision-banner ${s.counts.critical?'critical':s.counts.high?'high':s.counts.failed?'moderate':'low'}"><span>Decision</span><strong>${escapeHtml(simulation?'SIMULATION — NOT TARGET EVIDENCE':s.decision)}</strong></div><div class="assurance-meta"><span>Environment: ${escapeHtml(run.campaign?.environment)}</span><span>Target mode: ${escapeHtml(run.campaign?.target?.mode)}</span><span>Runner ${escapeHtml(run.runnerVersion)}</span><span>Policy ${escapeHtml(run.policyVersion)}</span><span>Digest ${escapeHtml(String(run.digest).slice(0,18))}…</span></div>${deltaHtml(run.delta)}</section><section class="panel section-gap"><div class="section-heading compact-heading"><div><span class="eyebrow">Case evidence</span><h2>Adversarial outcomes</h2></div></div><div class="finding-stack">${run.results.map(caseHtml).join('')}</div></section><section class="panel section-gap"><h2>Evidence boundary</h2><p>${escapeHtml(run.trust?.boundary)}</p><ul class="check-list"><li>Signature verified: ${run.signatureValid?'yes':'no'}</li><li>Raw prompts uploaded: no</li><li>Raw target responses uploaded: no</li><li>Customer credentials uploaded: no</li><li>Production testing allowed: no</li></ul></section>`}
function caseHtml(x){return `<article class="finding-card"><div class="finding-head"><div><span class="severity ${escapeHtml(x.severity)}">${escapeHtml(x.severity)}</span><h3>${escapeHtml(x.caseId)} · ${escapeHtml(x.title)}</h3></div><span class="outcome ${escapeHtml(x.outcome)}">${escapeHtml(x.outcome)}</span></div><p class="muted">${escapeHtml(x.category)} · confidence ${escapeHtml(x.confidence)} · ${x.durationMs} ms</p>${x.evidence.map(e=>`<div class="evidence-line"><strong>${escapeHtml(e.type)}</strong><span>${escapeHtml(e.fact)}</span></div>`).join('')}${x.outcome!=='passed'?`<div class="recommendation"><strong>Required remediation</strong><p>${escapeHtml(x.remediation)}</p></div>`:''}${x.frameworks?.length?`<div class="tag-list">${x.frameworks.map(f=>`<span>${escapeHtml(f)}</span>`).join('')}</div>`:''}</article>`}
function deltaHtml(d){if(!d||d.status==='first-run')return '<p class="muted">This is the first recorded run for the assessment.</p>';return `<div class="delta-grid"><div><span>Newly failed</span><strong>${d.newlyFailed?.length||0}</strong></div><div><span>Resolved</span><strong>${d.resolved?.length||0}</strong></div><div><span>Risk change</span><strong>${d.riskChange>=0?'+':''}${d.riskChange}</strong></div><div><span>Assurance change</span><strong>${d.assuranceChange>=0?'+':''}${d.assuranceChange}</strong></div></div>`}
function fail(message){root.className='panel';root.innerHTML=`<div class="error-box show">${escapeHtml(message)}</div>`}init();
