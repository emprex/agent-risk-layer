name: Temporary isolated Control Intelligence validation

on:
  push:
    branches:
      - validate/control-intelligence-isolated

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Run isolated Control Intelligence tests
        run: NODE_ENV=test node --test tests/control-intelligence.test.js
